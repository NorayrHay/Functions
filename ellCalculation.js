'use strict'

function ellCalculation(array){
    // let sum = 0;
    // let diff = 0;
    // let mult = 1;  
  
    for (let i = 0; i < array.length; i++) {
      // sum += array[i];
      // diff -= array[i];
      // mult *= array[i];
  
      // else  return  'infinity';
    }
    // res = '' + sum + diff + mult + div; 
    // return div;
  }
  console.log(ellCalculation([2, 0, -7, 10]));